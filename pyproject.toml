[project]
name = "threat-ip-map"
version = "0.1.0"
description = "SOC-панель: тепловая карта вредоносных IP (D3, офлайн)"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
]
