[project]
name = "threat-ip-map"
version = "0.1.0"
description = "Тепловая карта вредоносных IP — демо вайб-кодинга для РосИнфоБез"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.32",
]
